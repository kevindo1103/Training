name: Deploy Production

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  quality-gate:
    uses: ./.github/workflows/pr-quality-gate.yml
    secrets: inherit

  deploy-backend-production:
    runs-on: ubuntu-latest
    needs: quality-gate
    if: github.event.repository.fork == false
    env:
      VPS_HOST: ${{ secrets.VPS_HOST }}
      VPS_USER: ${{ secrets.VPS_USER }}
      VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      FACILITATOR_SECRET: ${{ secrets.FACILITATOR_SECRET }}
      DATABASE_URL: ${{ secrets.DATABASE_URL_PRODUCTION }}
      CORS_ORIGINS: ${{ secrets.CORS_ORIGINS_PRODUCTION }}
      JWT_ALGORITHM: ${{ secrets.JWT_ALGORITHM }}
      JWT_EXPIRE_MINUTES: ${{ secrets.JWT_EXPIRE_MINUTES }}
    steps:
      - uses: actions/checkout@v4

      - name: Check backend exists
        id: detect
        run: |
          if [[ -f backend/requirements.txt ]]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "backend/requirements.txt not found; skipping backend deploy."
          fi

      - name: Configure SSH
        if: steps.detect.outputs.exists == 'true'
        run: |
          mkdir -p ~/.ssh
          printf '%s' "$VPS_SSH_KEY" | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Bootstrap VPS directories
        if: steps.detect.outputs.exists == 'true'
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes "${VPS_USER}@${VPS_HOST}" << 'EOF'
            set -e
            id deploy >/dev/null 2>&1 || sudo useradd -r -s /bin/false deploy
            sudo mkdir -p /opt/dolphin-training/backend
            sudo mkdir -p /opt/dolphin-training/data
            sudo chown -R deploy:deploy /opt/dolphin-training
          EOF

      - name: Sync backend to VPS production
        if: steps.detect.outputs.exists == 'true'
        run: |
          rsync -avz --delete \
            --exclude='venv' \
            --exclude='.env' \
            --exclude='*.db' \
            --exclude='*.db-shm' \
            --exclude='*.db-wal' \
            --exclude='__pycache__' \
            --exclude='.pytest_cache' \
            --exclude='.mypy_cache' \
            --exclude='.git' \
            -e "ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes" \
            backend/ "${VPS_USER}@${VPS_HOST}:/opt/dolphin-training/backend/"

      - name: Install dependencies and restart service
        if: steps.detect.outputs.exists == 'true'
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes "${VPS_USER}@${VPS_HOST}" << EOF
            set -e
            cd /opt/dolphin-training/backend
            python3 -m venv venv
            source venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt

            {
              printf '%s\n' \
                "JWT_SECRET=${JWT_SECRET}" \
                "FACILITATOR_SECRET=${FACILITATOR_SECRET}" \
                "DATABASE_URL=${DATABASE_URL}" \
                "CORS_ORIGINS=${CORS_ORIGINS}" \
                "ENV=production"
              [[ -n "${JWT_ALGORITHM}" ]] && printf 'JWT_ALGORITHM=%s\n' "${JWT_ALGORITHM}"
              [[ -n "${JWT_EXPIRE_MINUTES}" ]] && printf 'JWT_EXPIRE_MINUTES=%s\n' "${JWT_EXPIRE_MINUTES}"
            } > .env
            chmod 600 .env

            if [[ -z "${DATABASE_URL}" ]]; then
              echo "ERROR: DATABASE_URL is empty. Check the DATABASE_URL_PRODUCTION secret."
              exit 1
            fi

            echo "--- diagnostics ---"
            echo "PWD=$(pwd)"
            echo "DATABASE_URL length: ${#DATABASE_URL}"
            echo "DATABASE_URL prefix: ${DATABASE_URL:0:15}"
            DB_PATH=$(python3 -c "from urllib.parse import urlparse; import sys; print(urlparse(sys.argv[1]).path)" "$DATABASE_URL")
            echo "DATABASE_URL path: $DB_PATH"
            ls -la /opt/dolphin-training/ || true
            ls -la /opt/dolphin-training/data/ || true
            echo "--- .env (redacted) ---"
            sed 's/=.*$/=***/' .env
            echo "--- end diagnostics ---"

            DB_DIR=$(python3 -c "from urllib.parse import urlparse; import os, sys; url=sys.argv[1]; print(os.path.dirname(urlparse(url).path))" "$DATABASE_URL")
            echo "Ensuring DB directory: $DB_DIR"
            sudo mkdir -p "$DB_DIR"
            sudo chown -R deploy:deploy "$DB_DIR"
            sudo chmod 775 "$DB_DIR"

            alembic upgrade head
            sudo chown -R deploy:deploy "$DB_DIR"
            sudo systemctl restart dolphin-training-backend
          EOF

      - name: Health check
        if: steps.detect.outputs.exists == 'true' && env.PRODUCTION_HEALTH_URL != ''
        env:
          PRODUCTION_HEALTH_URL: ${{ secrets.PRODUCTION_HEALTH_URL }}
        run: |
          for i in {1..5}; do
            if curl -sf "$PRODUCTION_HEALTH_URL"; then
              echo "Health check OK"
              exit 0
            fi
            echo "Attempt $i failed; retrying..."
            sleep 5
          done
          echo "Health check failed: $PRODUCTION_HEALTH_URL"
          exit 1

      - name: Cleanup SSH key
        if: always()
        run: rm -f ~/.ssh/deploy_key
